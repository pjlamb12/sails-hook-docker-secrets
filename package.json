{
	"name": "sails-hook-docker-secrets",
	"version": "1.0.0",
	"description": "Sails Hook to load docker secrets as a JSON object and to merge that into the sails.config object",
	"main": "index.js",
	"scripts": {
		"mocha": "mocha",
		"test": "npm run mocha -R ./test"
	},
	"keywords": ["sails", "hook", "docker", "secrets"],
	"sails": {
		"isHook": true
	},
	"author": "preston.j.lamb@gmail.com",
	"license": "MIT",
	"devDependencies": {
		"mocha": "^5.0.4",
		"sails": "^0.12.14"
	},
	"dependencies": {
		"@cloudreach/docker-secrets": "^1.0.3",
		"lodash.merge": "^4.6.1"
	}
}
